import { AppBar, CssBaseline, Toolbar } from "@mui/material";
import { Root, Content, ToolbarSpacer } from "./navstayles";

const NavBar = () => {

  return (
    <>
      <AppBar position="fixed">
        <Toolbar></Toolbar>
      </AppBar>
    </>
  );
};
export default NavBar;
